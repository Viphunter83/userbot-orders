# 📖 Руководство пользователя: Telegram Orders Bot

## 🚀 Быстрый старт

### Запуск userbot
```bash
python3 -m src.main start
```

**Важно:** Userbot работает в фоновом режиме и обрабатывает сообщения автоматически. **НЕ нужно прерывать процесс** для выполнения других команд!

---

## 📋 Доступные команды

### 1. 📊 Статистика и Dashboard

#### Просмотр dashboard (красивый вывод в терминале)
```bash
python3 -m src.main stats dashboard --period week
```
- `--period`: `week`, `month`, или `all`
- Показывает метрики, топ категории, топ авторов, статус системы

#### Экспорт метрик в CSV
```bash
python3 -m src.main stats export --period week
```
- Создает файлы: `metrics_daily_*.csv` и `metrics_categories_*.csv`
- Сохраняется в `./exports/`

#### Сводный отчет (JSON)
```bash
python3 -m src.main stats summary --period month
```

---

### 2. 📤 Экспорт заказов

#### Экспорт в CSV
```bash
python3 -m src.main export csv --period week --category Backend
```
- `--period`: `today`, `week`, `month`, `all`
- `--category`: опционально, фильтр по категории (Backend, Frontend, Mobile, AI/ML, Low-Code, Other)
- `--output-dir`: директория для сохранения (по умолчанию `./exports/`)

#### Экспорт в HTML (интерактивная таблица)
```bash
python3 -m src.main export html --period week
```
- Создает интерактивный HTML файл с фильтрами и сортировкой
- Открой файл в браузере для просмотра

---

### 3. 💬 Управление чатами

#### Просмотр всех чатов
```bash
python3 -m src.main chat list
```

#### Добавить чат вручную
```bash
python3 -m src.main chat add -1001234567890 --name "Название чата" --chat-type group --priority 3
```
- `--priority`: 1-5 (5 = наивысший приоритет)

#### Автоматическое обнаружение чатов
```bash
python3 -m src.main chat auto-detect
```
- Показывает все доступные чаты
- Интерактивный выбор для добавления

#### Включить/отключить чат
```bash
python3 -m src.main chat enable -1001234567890
python3 -m src.main chat disable -1001234567890 --reason "Временно отключен"
```

#### Установить приоритет
```bash
python3 -m src.main chat priority -1001234567890 5
```

#### Удалить чат из мониторинга
```bash
python3 -m src.main chat remove -1001234567890
```

---

### 4. ⚙️ Административные команды

#### Инициализация базы данных
```bash
python3 -m src.main admin init-db
```
- Создает все необходимые таблицы в Supabase

#### Проверка подключения к БД
```bash
python3 -m src.main admin test-connection
```

---

## 🔄 Работа во время выполнения userbot

### ✅ Что можно делать БЕЗ остановки userbot:

1. **Просматривать статистику**
   ```bash
   python3 -m src.main stats dashboard --period week
   ```

2. **Экспортировать данные**
   ```bash
   python3 -m src.main export csv --period today
   python3 -m src.main export html --period week
   ```

3. **Управлять чатами**
   ```bash
   python3 -m src.main chat list
   python3 -m src.main chat add ...
   python3 -m src.main chat enable/disable ...
   ```

4. **Проверять метрики**
   ```bash
   python3 -m src.main stats summary --period week
   ```

### ❌ Что требует остановки userbot:

- Изменение настроек в `.env` (требует перезапуска)
- Изменение кода (требует перезапуска)

---

## 📁 Где хранятся данные

### База данных (Supabase)
- **Сообщения**: таблица `userbot_messages`
- **Заказы**: таблица `userbot_orders`
- **Статистика**: таблица `userbot_stats`
- **Чаты**: таблица `userbot_chats`

### Локальные файлы
- **Экспорты**: `./exports/`
  - CSV файлы: `orders_*.csv`, `metrics_*.csv`
  - HTML файлы: `orders_*.html`
- **Конфигурация чатов**: `./config/chats.json`
- **Логи**: выводятся в терминал (можно перенаправить в файл)

---

## 📊 Примеры использования

### Ежедневная проверка
```bash
# 1. Посмотреть dashboard за сегодня
python3 -m src.main stats dashboard --period week

# 2. Экспортировать заказы за сегодня
python3 -m src.main export csv --period today

# 3. Посмотреть сводку
python3 -m src.main stats summary --period week
```

### Еженедельный отчет
```bash
# 1. Dashboard с метриками
python3 -m src.main stats dashboard --period week

# 2. Экспорт всех данных
python3 -m src.main export csv --period week
python3 -m src.main export html --period week
python3 -m src.main stats export --period week

# 3. Открыть HTML в браузере
open exports/orders_*.html  # macOS
# или
xdg-open exports/orders_*.html  # Linux
```

### Управление чатами
```bash
# 1. Посмотреть текущие чаты
python3 -m src.main chat list

# 2. Добавить новый чат
python3 -m src.main chat add -1001234567890 --name "Новый чат" --priority 5

# 3. Отключить чат с низким приоритетом
python3 -m src.main chat disable -1001111111111 --reason "Мало заказов"
```

---

## 🎯 Типичные сценарии

### Сценарий 1: Первый запуск
```bash
# 1. Инициализировать БД
python3 -m src.main admin init-db

# 2. Добавить чаты
python3 -m src.main chat auto-detect

# 3. Запустить userbot
python3 -m src.main start
```

### Сценарий 2: Ежедневная работа
```bash
# Userbot работает в фоне (не прерываем!)

# В другом терминале:
# 1. Проверить статистику
python3 -m src.main stats dashboard --period week

# 2. Экспортировать новые заказы
python3 -m src.main export csv --period today

# 3. Посмотреть в браузере
open exports/orders_*.html
```

### Сценарий 3: Добавление нового чата
```bash
# Userbot работает в фоне

# В другом терминале:
python3 -m src.main chat add -1001234567890 --name "Новый чат" --priority 3

# Userbot автоматически начнет мониторить новый чат!
```

---

## 🔍 Просмотр данных в Supabase

### Через Supabase Dashboard
1. Открой [Supabase Dashboard](https://app.supabase.com)
2. Выбери проект
3. Перейди в **Table Editor**
4. Просматривай таблицы:
   - `userbot_messages` - все сообщения
   - `userbot_orders` - обнаруженные заказы
   - `userbot_stats` - статистика

### Через SQL запросы
```sql
-- Последние 10 заказов
SELECT * FROM userbot_orders 
ORDER BY created_at DESC 
LIMIT 10;

-- Заказы по категориям
SELECT category, COUNT(*) as count 
FROM userbot_orders 
GROUP BY category 
ORDER BY count DESC;

-- Статистика за сегодня
SELECT * FROM userbot_stats 
WHERE date = CURRENT_DATE;
```

---

## 💡 Полезные советы

1. **Не прерывайте userbot** - он работает в фоне и обрабатывает сообщения автоматически
2. **Используйте dashboard** для быстрого просмотра статистики
3. **Экспортируйте данные регулярно** для анализа в Excel/Google Sheets
4. **HTML экспорт** удобен для просмотра в браузере с фильтрами
5. **Управляйте приоритетами чатов** - важные чаты получают приоритет 5
6. **Проверяйте логи** - они показывают что происходит в реальном времени

---

## 🆘 Решение проблем

### Userbot не обрабатывает сообщения
```bash
# Проверь какие чаты в мониторинге
python3 -m src.main chat list

# Проверь подключение к БД
python3 -m src.main admin test-connection
```

### Нужно перезапустить userbot
```bash
# 1. Останови (Ctrl+C)
# 2. Запусти снова
python3 -m src.main start
```

### Ошибки при экспорте
```bash
# Проверь что есть данные
python3 -m src.main stats dashboard --period week

# Если данных нет - подожди пока userbot соберет их
```

---

## 📞 Полный список команд

```bash
# Главная команда
python3 -m src.main --help

# Статистика
python3 -m src.main stats dashboard --period week
python3 -m src.main stats export --period week
python3 -m src.main stats summary --period month

# Экспорт
python3 -m src.main export csv --period week
python3 -m src.main export html --period week

# Управление чатами
python3 -m src.main chat list
python3 -m src.main chat add ...
python3 -m src.main chat remove ...
python3 -m src.main chat enable ...
python3 -m src.main chat disable ...
python3 -m src.main chat priority ...
python3 -m src.main chat auto-detect

# Администрирование
python3 -m src.main admin init-db
python3 -m src.main admin test-connection
```

---

**Удачной работы! 🚀**

